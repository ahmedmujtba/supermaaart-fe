import {
  StyleSheet,
  Text,
  View,
  TextInput,
  Button,
  ImageBackground,
} from "react-native";
import { useState } from "react";

export default function Home({ navigation }) {
  return (
    <View>
      <Text>Home page</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    width: 400,
    height: 720,
  },
});
